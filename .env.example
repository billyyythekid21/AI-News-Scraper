# Local defaults, copy values into a real .env (gitignored) as needed.
# Frontend (Vite), bake in at build time for production:
# VITE_API_URL=https://your-api.example.com

# Backend
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/csoc
# CORS_ORIGINS=https://your-frontend.example.com
# GOOGLE_API_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_PUBLIC_KEY=
# GITHUB_TOKEN=
